*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.idea/
.kotlin
**/build/
/bin
**/bin/

# NOTE: previously this file ignored ALL markdown (**/*.md), which hid the
# README and every doc from git/GitHub. Removed so portfolio docs are tracked.
# If you want to hide specific internal notes, ignore them by explicit path instead.

# Firebase config — keep your real project config out of the public repo.
# Use app/google-services.json.example as a template.
app/google-services.json